---
title: Python 变量简介
---

# Python 变量

变量用于存储数据。你可以用等号 `=` 给变量赋值：

```python
x = 5
y = "Hello"
```

变量名可以是字母、数字和下划线的组合，但不能以数字开头。

- 有意义的变量名有助于代码可读性。
- Python 是动态类型语言，变量类型可以随时改变。

> 试着在交互式解释器里定义几个变量吧！
